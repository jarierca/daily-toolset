# Usa la imagen oficial de Node.js como base
FROM node:latest

# Actualiza npm a la última versión
RUN npm install -g npm@10.4.0

# Establece el directorio de trabajo dentro del contenedor
WORKDIR /app

# Copia el archivo package.json y package-lock.json (si existe)
COPY package*.json ./

# Forzar la instalación de dependencias, evitando los problemas de resolución de dependencias
RUN npm install --force

# Copia el archivo app.js desde la carpeta src a /app en el contenedor
COPY src/App.js .

# Expone el puerto 8080
EXPOSE 8080

# Comando para ejecutar la aplicación
CMD ["node", "App.js"]  # Reemplaza "app.js" con el nombre del archivo principal de tu aplicación

